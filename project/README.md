$ mkdir Romakh_DK21-mp
$ cd Romakh_DK21-mp/
$ git init
$ mkdir project
$ cd project/
$ touch README.md
$ git status
$ git add project/
$ git commit -m "subtask 1"

$ git branch first_branch
$ git checkout first_branch
$ git status
$ git add project/README.md
$ git status

$ git checkout master
$ git add project/README.md
$ git commit -m "subtask 4"
$ git log
